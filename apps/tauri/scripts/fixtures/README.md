# Script Fixtures

This directory contains data files consumed by maintainer-run scripts and
manual validation checklists. They are not executable entrypoints.

- `seed.sql` seeds a small realistic development database.
- `seed_scale.sql` seeds synthetic 1k/10k task datasets for scale checks.
