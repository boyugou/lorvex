# Distribution

## Distribution Channels

| Channel | Status | Notes |
| --- | --- | --- |
| GitHub Releases (macOS DMG) | Developer/reference | Ships `.dmg` artifacts for contributor smoke testing. |
| GitHub Releases (Windows EXE) | Implemented | Builds `.exe` artifacts. |
| GitHub Releases (Linux AppImage + .deb + .rpm) | Implemented | Builds `.AppImage`, `.deb`, and `.rpm` artifacts. |
| Homebrew Cask | Planned | Follows the release artifact feed. |
| Mac App Store | Superseded for Tauri | Ship the Swift app from `apps/apple` instead. |
| iOS / iPadOS | Superseded for Tauri | Ship the Swift app from `apps/apple` instead. |
| Android | Future | Future non-Apple mobile runtime. |

Artifacts include .dmg, .exe, .AppImage, .deb, .rpm.
