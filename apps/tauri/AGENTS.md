# AGENTS.md

This file is intentionally a one-screen redirect.

The canonical agent operating instructions for this repository live in
[`CLAUDE.md`](CLAUDE.md). That file is the single source of truth for
project structure, coding standards, design rules, and the development
workflow every coding agent must follow.

`AGENTS.md` exists as a discoverability anchor for tooling that probes
for the conventional `AGENTS.md` filename. Read `CLAUDE.md` instead —
do not maintain two copies in lockstep.
