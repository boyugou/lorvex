{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-theme",
  "description": "Allow app-level native theme changes only from the main window",
  "windows": ["main"],
  "permissions": [
    "core:app:allow-set-app-theme"
  ]
}
