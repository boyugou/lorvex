import Foundation

// Intentionally empty. Every LorvexTaskServicing requirement is implemented
// directly by its conformers (SwiftLorvexCoreService);
// no protocol-extension default stubs live here, so a missing implementation is a
// compile error rather than a silent runtime throw. Any new conformer must
// implement every requirement directly.
