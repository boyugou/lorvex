import { AppSelect } from '../../../ui/AppSelect';

export function SelectPass() {
  return (
    <AppSelect
      aria-label="scope"
      value="general"
      onChange={() => {}}
      variant="inline"
      options={[]}
    />
  );
}
